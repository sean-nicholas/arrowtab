{
  "name": "arrowtab",
  "version": "0.0.2",
  "description": "",
  "main": "dist/index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"tsc --watch\" \"http-server -o\"",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublish": "pnpm clean && pnpm build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "typescript": "^5.5.4"
  }
}
